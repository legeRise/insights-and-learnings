Notes: there are two ways to put static foler  in jango apps an project  
1.  I keep a single static folder  in basedir where i have manage.py as well
2. i keep static in each app separately so like each app has static in the same dir where templates foler is an then inside there is my app's named folder 

and to this point what i have understood 

 if  I follow the second approach I no longer require staticfilerdirs cause it already knows by default where to look for static just like if we do the same
 for templates while if we follow first method we have to tell django where to look for static folder ---in the staticfiledirs same is the case for templates
 folder but the only different in that settings.py already provides us the list  same correspoing to staticfileir for templates an we put templates path in i



STATIC URL ROOT DIRS ALL IN ALL:

oh so techniccally there will be a folder collectstatic folder  named somedthing where django would collect all static files the name of folders can be anything
staticfiles,static or staticroot whatever is it the static root will always have  os.path.join(basedir,foldername) structure 

but usually as a convention we set 'staticfiles' as a STATIC_ROOT = os.path.join(BASE_DIR, 'static_root')  static root because    doing static root ain't that 
famous while doing   basedir,static may have a possible conflict with existing staitfilesdirs because even after have apps' specfic static folders  still 
there is a big chance that  there will exist a common static folder at project ---serving all files to other apps so if we already have static folder ant hen
 we use it for collectstatic(staticroot ) too that might end us up with confilicts

so that is why we make folder staticfiles in mainproject and keep working with static folders at project ( if there is) and app level and staticfiles 
 folder remains there untouched however when we are done with are development and we are ready to set debug to false then we run collectstatic command 
and all static folders (app level or project) are collected into one staticfiles folder and from now on this would be used